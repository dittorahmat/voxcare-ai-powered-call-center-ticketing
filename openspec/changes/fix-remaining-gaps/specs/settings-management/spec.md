## MODIFIED Requirements

### Requirement: Settings Navigation Placement
The Settings navigation item SHALL be moved from the sidebar footer to the main navigation group, appearing after "Calls". It SHALL link to `/settings/profile` and be visible to all authenticated users.

#### Scenario: Settings in main nav
- **WHEN** any authenticated user views the sidebar
- **THEN** "Settings" appears as a main navigation item after "Calls"
